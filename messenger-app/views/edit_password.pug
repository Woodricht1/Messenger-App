html 
    head
        title Edit Password 
    body 
        div Welcome, !{username}. Set your new password below.
        div 
            p 
                form(action = "/edit_password" method = "POST")
                    input(name = "password" type = "password" required placeholder = "Password")
                    button(type = "Submit") Change password